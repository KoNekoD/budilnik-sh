# Budilnik-Sh

This is a shell script that acts as an alarm clock.
To use it, simply enter the time at which you want it to activate and leave the terminal and computer turned on. 
At the specified time, the alarm clock will activate and wake the target user.

## Usage

```shell
./alarm.sh
```
